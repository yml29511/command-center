请对以下企业名单执行CRM状态核查任务。

输入数据：
${Prohibited cooperation}

执行要求：
1. 解析上方JSON中的 company_list 企业列表。
2. 如果企业数量为0，直接返回空结果JSON。
3. 将企业列表按每组最多5个进行分批。
4. 对每一批，通过 @无影-预发-同步 调用BUA执行核查，将该批企业JSON数组作为变量 ${batch_company_list} 传入。
5. 每批次如果失败，最多重试3次（递增退避10s/20s），3次均失败则标记失败并继续下一批。
6. 全部批次执行完毕后，汇总所有批次返回的 abnormal_companies（状态为"开通"即"未关闭"的企业），合并为统一的JSON结果输出。

输出要求：
- 仅输出纯JSON，严禁Markdown格式或任何解释性文字。
- 全部成功则 status 为 "success"，存在失败批次则为 "partial_success"。
